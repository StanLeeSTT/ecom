{
  "name": "legimi-commerce",
  "version": "0.1.0",
  "description": "AI-native commerce operating system",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter='./packages/*' install && pnpm --filter='./apps/*' --filter='./apps/*' --parallel run dev",
    "dev:api": "pnpm --filter='./apps/api' run dev",
    "dev:web": "pnpm --filter='./apps/web' run dev",
    "dev:storefront": "pnpm --filter='./apps/storefront' run dev",
    "build": "pnpm --filter='./packages/*' build && pnpm --filter='./apps/*' build",
    "build:deps": "pnpm --filter='./packages/*' build",
    "build:apps": "pnpm --filter='./apps/*' build",
    "test": "pnpm --filter='./packages/*' --filter='./apps/*' test",
    "lint": "pnpm --filter='./packages/*' --filter='./apps/*' lint",
    "type-check": "pnpm --filter='./packages/*' --filter='./apps/*' type-check",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "perf:check": "bash scripts/performance-check.sh"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
